# N-Gram Language Model

This is a simple bigram language model using Python and NLTK.  
It reads a sample text, tokenizes it, and calculates bigram probabilities with Laplace smoothing.

## Run it:
```bash
python main.py
